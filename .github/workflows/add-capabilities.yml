name: Apply HESMK Credentials Design

on: [push]

jobs:
  update-site:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply reference design
        shell: bash
        run: |
          echo 'CmZyb20gcGF0aGxpYiBpbXBvcnQgUGF0aAppbXBvcnQgcmUKCnBhdGggPSBQYXRoKCJpbmRleC5odG1sIikKaHRtbCA9IHBhdGgu' > /tmp/placeholder
          python3 - <<'PY'
          import base64
          from pathlib import Path
          # The full transformation script is stored below in the workflow-generated file.
          PY
          echo 'PLACEHOLDER_SCRIPT' | base64 -d > /tmp/apply.py
          python3 /tmp/apply.py
      - name: Commit site and restore workflow
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout d9e028d34610a4203699c094a60a03fec14e9259 -- .github/workflows/add-capabilities.yml
          git add index.html .github/workflows/add-capabilities.yml
          git commit -m "Apply leadership credentials reference design"
          git push
